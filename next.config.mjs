/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['res.cloudinary.com','m.media-amazon.com','th.bing.com'],  // Add the allowed domains here
    },
  };
  
  export default nextConfig;
  